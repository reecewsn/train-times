import TimetableResults from "./TimetableResults";

const Timetable = ({ station }) => {
    return (
        <div>
            <TimetableResults station={station} />
        </div>
    )
}

export default Timetable;
